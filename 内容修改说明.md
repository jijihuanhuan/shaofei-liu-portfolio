# 个人网站内容修改说明

网站内容都写在源码的**数据文件 / 组件文件**里，改完后需要提交推送到 GitHub 才会更新到线上。

## 一、内容在哪个文件（速查）

| 要改的内容 | 文件 |
|---|---|
| 侧边栏口号、导航文字、底部版权 | `app/components/SiteShell.tsx` |
| 首页（按年份列出的条目） | `app/components/HomePage.tsx` |
| 简历页（基本信息 / 教育 / 技能 / 项目 / 荣誉） | `app/components/ResumePage.tsx` |
| 研究页（研究方向 / 当前课题） | `app/components/ResearchPage.tsx` |
| 项目页（项目经历） | `app/components/ProjectsPage.tsx` |
| 摩托车 / 摄影 / 烹饪 / 挚友 | `app/data/life.ts` |
| 年度总结 | `app/components/AnnualPage.tsx` |
| 关于页（我是谁 / 喜欢啥 / 音乐 / 书籍 / 影视） | `app/components/AboutPage.tsx` |
| 简历照片 | `public/portrait.jpg` |
| 侧边栏头像（线条插画） | `public/avatar.svg` |
| 网站标题 / 描述（浏览器标签页） | `app/layout.tsx` |

所有文件路径都基于项目根目录：
`/Users/nanxing/Documents/个人简历项目`

---

## 二、各文件的具体格式

### 1. 侧边栏口号 / 导航文字 / 版权 — `SiteShell.tsx`

- 口号（slogan）在 `SiteShell` 函数里：
  ```tsx
  const slogan =
    lang === "zh" ? "学会独处，连接世界" : "Learn to be alone, connect to the world";
  ```
- 导航文字在文件上方的 `zhLabels`（中文）和 `enLabels`（英文）里。
- 底部版权文案在 `<footer className="footer">` 里。

### 2. 首页年份列表 — `HomePage.tsx`

结构：`["年份", [ ["分类", "标题", "链接地址"], ... ]]`

```ts
const zhEntries: YearGroup[] = [
  ["2025", [
    ["研究", "面向生成式模型的 VVC 张量压缩与计算旁路", "/zh/research"],
    ["项目", "硬件设计空间探索与 PPA 预测框架", "/zh/projects"],
  ]],
  ["2024", [
    ["项目", "数字电路设计与 FPGA 验证", "/zh/projects"],
  ]],
];
```

- 第一个是年份，第二个是该年份下的条目数组。
- 每个条目是 3 个值：`分类`、`标题`、`跳转地址`。
- 中文放 `zhEntries`，英文放 `enEntries`，年份顺序按「新到旧」排。

### 3. 简历页 — `ResumePage.tsx`

顶部有 `const data = { zh: {...}, en: {...} }`，改里面的文字即可：

| 字段 | 含义 |
|---|---|
| `name` | 姓名 |
| `basic` | 「基本信息」标题 |
| `info` | 基本信息，格式 `["标签", "内容"]` |
| `edu` | 「教育背景」标题 |
| `education` | 每条 `["起止时间", "学校", "专业 · GPA"]` |
| `skills` | 「专业技能」标题 |
| `skillGroups` | 每条 `["技能名", "描述"]` |
| `projects` | 「项目经历」标题 |
| `projectItems` | 每条 `["项目名", "描述", "技术栈"]` |
| `honors` | 「荣誉与社会经历」标题 |
| `honorItems` | 荣誉，每项一个字符串 |

简历照片固定读 `public/portrait.jpg`，替换这张图即可（保持文件名不变，或改 `ResumePage.tsx` 里的 `portrait.jpg`）。

### 4. 研究页 — `ResearchPage.tsx`

```ts
const data = {
  zh: {
    title: "研究",
    intro: "……",
    directions: "核心研究方向",
    directionItems: ["……", "……"],
    current: "当前课题",
    currentTitle: "……",
    currentDesc: "……",
  },
  en: { ... },
};
```

### 5. 项目页 — `ProjectsPage.tsx`

每个项目一个对象：

```ts
{
  name: "项目名",
  status: "研究项目 · 进行中",
  desc: "一句话描述",
  points: ["要点1", "要点2", "要点3"],   // 圆点列表
  tags: "gem5 · CACTI · Python",
}
```

### 6. 摩托车 / 摄影 / 烹饪 / 挚友 — `app/data/life.ts`

四个数组分别对应四页：`motorcycleItems`、`photographyItems`、`cookingItems`、`friendsItems`。

每条格式：

```ts
{
  title: "第一次长途",              // 必填：标题
  date: "2025-10",                 // 可选：日期
  description: "北京—张家口，两天。", // 可选：说明
  image: "/motorcycle-1.jpg",      // 可选：图片（见下方图片说明）
  link: "https://……",              // 可选：外链或站内链接
}
```

- **摄影页**是图片墙，只需要 `title` 和 `image`。
- **摩托车 / 烹饪 / 挚友**是图文列表，`title` 必填，其余可选。

### 7. 年度总结 — `AnnualPage.tsx`

纯文字页面，直接改 `<h2>`、`<h3>`、`<p>`、`<ul><li>` 里的文字即可。

### 8. 关于页 — `AboutPage.tsx`

同样直接改文字。结构和小标题目前是：

- 我是谁？
- 喜欢啥？
  - 音乐（民谣 / 摇滚）
  - 书籍
  - 影视

### 9. 网站标题 / 描述 — `layout.tsx`

```ts
title: { default: "刘绍飞 | 数字 IC 设计与 AI 软硬件协同", template: "%s | 刘绍飞" },
description: "……",
```

---

## 三、图片怎么放

1. 把图片文件放进 `public/` 目录。
2. 在内容里写图片路径时，用 `/文件名.jpg` 这种写法（以 `/` 开头）。

例：放入 `public/motorcycle-1.jpg`，则 `life.ts` 里写 `image: "/motorcycle-1.jpg"`。

---

## 四、改完怎么更新到线上

在项目根目录执行：

```bash
git add .
git commit -m "第一版"
git push origin main
```

推送到 `main` 后，GitHub 会自动构建并发布，约 1–2 分钟后刷新：

https://jijihuanhuan.github.io/shaofei-liu-portfolio/

---

## 五、注意

- 中文内容改 `zh`，英文内容改 `en`，两边各自独立。
- 改完可以先本地预览检查，再推送：
  ```bash
  npm run build
  npm run start
  ```
  然后浏览器打开 `http://localhost:3000`。
- `codex对话日志/` 目录只是历史对话备份，与网站无关，提交时可以不理会。
